var startDate; //date - asked for
    var endDate; //date, computed based upon startDate & byWeek
    var byWeek; //date
    var gameTime; //time, for completness
var numLocations; //int - asked for
    var nameLocation; //string
        var numTables; //int
        var addressLocation; //html5 address tag, string
        var phoneLocation; //html5 phone tag, int

var numTeams; //int
    var numWeeks; //int
    var numMatches; //int
    var teamNames; //string
    var teamLocAffinity;
    var teamLocH; // H means home. type: boolean


var x = numTeams - 1;

numMatches += x-1;

numWeeks = numTeams - 1;